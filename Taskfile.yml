version: "3"

tasks:
  default:
    cmds:
      - task: list

  list:
    desc: List all tasks in the current project
    cmds:
      - task --list

  ###################################################
  ############## Install Dependencies ###############
  ###################################################

  install:
    desc: Install all dependencies for the project
    cmds:
      - task: install:frontend
      - task: install:backend
      - task: install:desktop
      - task: build

  install:frontend:
    desc: Install dependencies for the frontend packages
    cmds:
      - pnpm install

  install:backend:
    desc: Install dependencies for the Rust backend
    cmds:
      - cargo fetch

  install:desktop:
    desc: Install dependencies for the desktop Tauri app (excluded from workspace)
    dir: apps/desktop/src-tauri
    cmds:
      - cargo fetch

  ###################################################
  ############## Clean and Build Tasks ##############
  ###################################################

  clean:
    desc: Clean build artifacts
    cmds:
      - rm -rf node_modules
      - |
        {{if eq OS "windows"}}
        pnpm -r exec -- powershell -NoProfile -Command "if (Test-Path node_modules) { Remove-Item -Recurse -Force node_modules }"
        {{else}}
        pnpm -r exec -- rm -rf node_modules
        {{end}}
      - pnpm store prune
      - cargo clean

  lint:
    desc: Run linters on all Rust crates and frontend packages
    cmds:
      - task: lint:backend
      - task: lint:frontend

  lint:backend:
    desc: Run linters on all Rust crates
    silent: true
    cmds:
      - cargo fmt --all
      - cargo clippy --workspace -- -D warnings

  lint:frontend:
    desc: Run linters on all frontend packages
    silent: true
    cmds:
      - pnpm --filter @ora/web-client lint
      - pnpm --filter @ora/desktop lint
      - pnpm --filter @ora/platform lint

  export-contracts:
    desc: Regenerate TypeScript contracts and the frontend SDK package
    cmds:
      - cargo xtask export-contracts

  build:
    desc: Build all crates
    cmds:
      - cargo build --workspace

  test:frontend:
    desc: Run TypeScript package test suites (contracts, chat, platform, app-shell, mock-service)
    cmds:
      - task: export-contracts
      - task: lint:frontend
      - pnpm --filter @ora/contracts test
      - pnpm --filter @ora/chat test
      - pnpm --filter @ora/platform test
      - pnpm --filter @ora/app-shell test
      - pnpm --filter @ora/mock-service test

  test:frontend:watch:
    desc: Re-run app-shell tests on file change
    cmds:
      - pnpm --filter @ora/app-shell test:watch

  test:backend:
    desc: Run Rust crate test suites
    cmds:
      - task: lint:backend
      - cargo test --workspace

  test:desktop:
    desc: Run the Desktop transport and independent Tauri Rust workspace tests
    cmds:
      - pnpm --filter @ora/desktop test
      - cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml -- --check
      - cargo clippy --manifest-path apps/desktop/src-tauri/Cargo.toml -- -D warnings
      - cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml

  test:
    desc: Run lint, regenerate contracts, then frontend and Rust tests
    silent: true
    cmds:
      - task: test:frontend
      - task: test:backend
      - task: test:desktop

  ###################################################
  ############# Run Development Servers #############
  ###################################################

  run:prepare:
    desc: Ensure the local .data workspace and rustun repository exist
    cmds:
      - mkdir -p .data
      - |
        if [ ! -d .data/rustun ]; then
          git clone https://github.com/ora-space/rustun.git .data/rustun
        fi

  run:web-frontend:
    desc: Run the web frontend against the real HTTP backend
    env:
      VITE_ORA_CONTRACT_TRANSPORT: fetch
    cmds:
      - pnpm --filter @ora/web-client dev

  run:web-proto:
    desc: Run the web frontend with the in-browser mock service
    env:
      VITE_ORA_CONTRACT_TRANSPORT: mock
    cmds:
      - pnpm --filter @ora/web-client dev

  run:ui-showcase:
    desc: Run the UI component showcase in development mode
    cmds:
      - pnpm --filter @ora/ui-showcase dev

  run:desktop:
    desc: Run the desktop Tauri app in development mode
    cmds:
      - pnpm --filter @ora/desktop tauri dev

  run:web-backend:
    desc: Run the web server backend in development mode
    env:
      ORA_DATA_DIR: .data
      ORA_PROJECT_NAME: rustun
      ORA_PROJECT_PATH: .data/rustun
      ORA_LOG_MODE: file
    cmds:
      - task: run:prepare
      - cargo run --bin ora_web_server

  run:logdy:file:
    desc: Follow Ora's file-only daily logs in the local Logdy UI
    preconditions:
      - sh: command -v logdy >/dev/null 2>&1
        msg: "Logdy is not available on PATH. Provide Logdy before running this task."
      - sh: ls .data/logs/ora.log.* >/dev/null 2>&1
        msg: "No Ora daily log files found in .data/logs (expected ora.log.YYYY-MM-DD). Start the file-backed backend first."
    cmds:
      - |
        set -- .data/logs/ora.log.*
        logdy follow --full-read "$@" --ui-ip 127.0.0.1 --port 8080 --no-analytics --no-updates --config logdy.config.json

  run:logdy:stdout-and-file:
    desc: Follow Ora's daily logs when the backend writes to stdout and files
    preconditions:
      - sh: command -v logdy >/dev/null 2>&1
        msg: "Logdy is not available on PATH. Provide Logdy before running this task."
      - sh: ls .data/logs/ora.log.* >/dev/null 2>&1
        msg: "No Ora daily log files found in .data/logs (expected ora.log.YYYY-MM-DD). Start the stdout_and_file backend first."
    cmds:
      - |
        set -- .data/logs/ora.log.*
        logdy follow --full-read "$@" --ui-ip 127.0.0.1 --port 8080 --no-analytics --no-updates --config logdy.config.json

  run:logdy:stdout:
    desc: Start the web backend through Logdy with stdout-only logging
    env:
      ORA_DATA_DIR: .data
      ORA_PROJECT_NAME: rustun
      ORA_PROJECT_PATH: .data/rustun
      ORA_LOG_MODE: stdout
    preconditions:
      - sh: command -v logdy >/dev/null 2>&1
        msg: "Logdy is not available on PATH. Provide Logdy before running this task."
    cmds:
      - task: run:prepare
      - logdy stdin "cargo run --bin ora_web_server" --ui-ip 127.0.0.1 --port 8080 --no-analytics --no-updates --config logdy.config.json
