# Ora 用户使用手册

## 1. Ora整体介绍

### 1.1 功能总览

Ora 以“项目”为工作范围，以“任务/会话”为主要使用入口，并通过 Agent、角色、Skill、插件和工作流扩展能力。

| 模块         | 解决的问题                                        | 主要产物或配置                  |
| ------------ | ------------------------------------------------- | ------------------------------- |
| 项目与任务   | 确定 Agent 在哪个目录工作，以及是否需要隔离工作区 | 直接任务、工作树任务            |
| 文件与变更   | 浏览项目文件，检查 Git 差异并执行提交或推送       | 文件预览、Diff、Git commit/push |
| 会话         | 与 Agent 交互，并向它提供文件、图片和扩展能力     | 消息、上下文、附件              |
| Agent 与模型 | 决定由哪个执行器和模型处理当前任务                | Agent、模型选择                 |
| 角色         | 为工作流中的 Agent 节点定义身份和行为             | Markdown 角色定义               |
| Skill        | 封装可复用的专业任务说明及配套资源                | `SKILL.md` 技能包               |
| 插件         | 向 Ora 安装新的 Agent、Skill、工具或页面          | `.orax` 插件包                  |
| 工作流       | 把多个步骤、分支和人工确认组织成可重复流程        | 工作流草稿与发布版本            |

### 1.2 界面总览

工作区页面介绍：

![image-20260828110652551](C:\Users\mds\AppData\Roaming\Typora\typora-user-images\image-20260828110652551.png)

## 2. 项目与任务

### 2.1 项目

项目用于指定当前工作的本地目录。会话中的文件选择、`@` 引用以及 Agent 的默认工作目录均受当前项目范围约束。

同一个项目可以包含多个任务。项目只是工作范围，不会自动复制、提交或修改其中的文件。

### 2.2 直接任务

“新建任务”会直接使用项目根目录。适合以下场景：

- 阅读、分析或修改当前项目；
- 继续处理本地已有但尚未提交的改动；
- 不需要为本次工作创建独立 Git 工作区。

直接任务与项目共享同一目录，因此不同任务修改的仍是同一份文件。并行运行多个会修改文件的任务时，需要自行避免改动互相覆盖。

### 2.3 工作树任务

“新建工作树任务”会为任务创建独立的 Git worktree。适合同时开发多个功能、隔离实验性修改或避免污染当前工作目录。

创建工作树任务需要：

- 项目目录是有效的 Git 仓库；
- 能够选择一个有效的基础分支；
- 本机 Git 可以正常创建 worktree。

工作树中的会话使用独立目录，修改不会直接写入项目原工作目录。删除工作树任务可能同时移除它对应的工作目录，因此应先确认其中的改动是否已经提交或另行保存。

### 2.4 两种任务的区别

| 对比项                     | 直接任务               | 工作树任务            |
| -------------------------- | ---------------------- | --------------------- |
| 工作目录                   | 项目根目录             | 独立 worktree         |
| 是否要求 Git               | 否                     | 是                    |
| 是否看到项目现有未提交改动 | 是                     | 通常否                |
| 适合场景                   | 日常处理、继续本地工作 | 并行开发、隔离修改    |
| 删除任务对文件的影响       | 不删除项目目录         | 可能移除对应 worktree |

## 3. 文件与 Git 变更

工作区中的“文件”和“变更”用于检查当前任务实际使用的目录。直接任务对应项目根目录；工作树任务对应该任务的独立 worktree。

### 3.1 文件

“文件”模块用于浏览和检查当前工作区内容，提供以下能力：

- 通过目录树浏览文件和文件夹；
- 按文件名或路径筛选目录内容；
- 按文件名搜索文件；
- 按关键字搜索文件内容；
- 只读预览文本和代码文件；
- 选择单行或连续多行，并将所选内容引用到当前会话；
- 刷新工作区文件列表。

搜索结果过多时，界面最多显示前 500 条。较大的文本文件仍可预览，但 Ora 会关闭语法高亮以维持界面性能。

该模块是只读浏览器，不提供直接编辑和保存功能。需要修改文件时，应由 Agent 执行，或使用外部编辑器处理。

### 3.2 变更

“变更”模块用于查看当前工作区的 Git 差异。该模块要求项目为有效 Git 仓库。

可查看的差异范围如下：

| 范围   | 显示内容                                                       |
| ------ | -------------------------------------------------------------- |
| 分支   | 当前工作区相对于任务基准提交的全部差异，包括已提交和未提交内容 |
| 未暂存 | 工作区相对于暂存区的差异，并包含未跟踪文件                     |
| 已暂存 | 暂存区相对于 `HEAD` 的差异                                     |
| 已提交 | `HEAD` 相对于任务基准提交的已提交差异                          |

“分支”和“已提交”需要任务创建时记录的基准提交，因此主要用于工作树任务。直接任务没有独立任务基准，只提供“未暂存”和“已暂存”范围。

差异查看器支持：

- 按目录查看变更文件，并按路径筛选；
- 查看新增、删除和修改的行；
- 在统一 Diff 与双栏 Diff 之间切换；
- 展开被折叠的未修改代码；
- 识别二进制文件变更以及仅元数据变更；
- 选择 Diff 中的一行或多行并引用到当前会话；
- 手动刷新变更状态。

引用 Diff 内容时，Ora 会将所选范围作为差异片段加入会话，使 Agent 获得文件路径、行号以及新增或删除信息。

### 3.3 提交与推送

“变更”模块提供以下 Git 写入操作：

- **提交**：填写提交说明后，为当前工作区创建 Git commit；
- **提交并推送**：完成提交后，将当前分支推送到远程仓库；
- **推送**：推送当前已有提交，不创建新提交。

当前版本不提供逐文件暂存、取消暂存或丢弃修改操作。执行“提交”时，Ora 会暂存并提交当前工作区中的全部未提交变更，而不是仅提交当前 Diff 中选中的文件。

推送属于对远程仓库的写入操作。确认后，Ora 会将当前分支推送到默认远程仓库，并在需要时设置上游分支。操作能否成功取决于远程仓库地址、Git 凭据和分支权限。

直接任务执行提交或推送时，作用对象是项目目录当前检出的分支；工作树任务执行这些操作时，作用对象是该任务的独立分支。

## 4. 会话

会话是使用 Agent 的主要入口。一个任务中可以保留和继续多个会话，每个会话维护自己的消息历史和上下文。

### 4.1 Agent 与模型

发送消息前可以选择 Agent 和模型：

- **Agent**：指定处理任务的执行器；可选项来自已安装且当前可用的 Agent 插件。
- **模型**：指定 Agent 使用的模型；不同 Agent 提供的模型列表可能不同。

### 4.2 文件与目录上下文

在输入区使用 `@` 可以选择当前项目中的文件或目录，并把它们作为明确上下文加入消息。适合指定需要阅读、修改或比较的内容。

`@` 引用与普通文字中的文件名不同：前者会建立可识别的项目资源引用，能减少 Agent 找错同名文件的情况。可选范围以当前项目为边界。

### 4.3 图片附件

会话支持 PNG、JPEG、WebP、GIF、AVIF 和 BMP 图片。单张图片最大 5 MB，同一条消息中的图片总量最大 10 MB。

图片可在发送前预览和移除。图片是否能被实际理解还取决于当前 Agent 与模型是否支持图像输入。

### 4.4 输入区快捷入口

当前输入区提供以下快捷入口：

| 入口 | 功能                                                 |
| ---- | ---------------------------------------------------- |
| `/`  | 选择并调用已安装且可用的 Skill                       |
| `@`  | 选择当前项目中的文件或目录，并将其作为上下文加入消息 |
| `+`  | 打开扩展操作菜单，可选择 Skill、插件或添加图片       |

图片还可以通过粘贴方式加入输入区。

Ora 在协议层支持接收 Agent 上报的 Commands。仅当当前 Agent 主动提供命令目录时，相关命令才会显示在快捷菜单中。

## 5. Agent 与模型

Ora 负责项目和会话管理，Agent 负责执行任务。Agent 通常由插件提供，因此 Settings 中不设置独立的 Agent 管理页面。

Agent 插件可以提供：

- 可用模型列表；
- 登录、密钥或运行环境配置；
- Agent 通过协议主动上报时可用的命令目录；
- 文件操作、终端或其他工具能力。

不同 Agent 或模型的能力范围可能不同。Agent 不可选时，常见原因包括对应插件未安装、插件被禁用、配置不完整或所需的本地运行环境不可用。

卸载提供 Agent 的插件后，已有会话记录仍可保留，但对应 Agent 可能无法继续运行。此时需要重新安装插件或改用其他 Agent 新建/继续任务。

## 6. 角色

角色用于为工作流中的 Agent 节点定义身份、职责、约束和工作方式，例如代码审查员、产品需求分析师或测试方案设计师。

角色本身不会运行。工作流执行到 Agent 节点时，由该节点选择的 Agent 和模型负责执行，并把所选角色作为身份与行为说明。角色不包含 Agent、模型、Skill 或 MCP 配置；这些能力分别在 Agent 节点的其他配置项中选择。

### 6.1 支持的能力

角色模块支持：

- 在 Ora 中新建和编辑角色；
- 从本地 Markdown 文件导入；
- 修改已导入角色；
- 删除不再需要的角色；
- 处理同名角色时选择跳过或覆盖；
- 在工作流的 Agent 节点中选择和复用。

### 6.2 角色导入格式

导入文件必须是一个 `.md` 文件，文件开头必须包含 YAML frontmatter，并提供非空的 `name` 和 `description`。frontmatter 后的 Markdown 正文就是角色内容。

```markdown
---
name: Code Reviewer
description: Reviews code changes for correctness and maintainability
---

# Responsibilities

- Review changed files.
- Identify correctness and maintainability risks.
- Return actionable findings with file locations.
```

格式要求：

- YAML frontmatter 必须位于文件开头，并由两行 `---` 包围；
- `name` 和 `description` 都必须存在且不能为空；
- 文件大小不能超过 1 MiB；
- 检测到同名角色时，导入流程要求选择跳过或覆盖，不会自动替换现有角色。

角色内容应只描述该 Agent 节点在工作流中以什么身份工作、负责什么、遵守什么规则。模型、Skill、MCP 和其他运行配置应在 Agent 节点中设置，不需要写进角色文件。

## 7. Skill

Skill 用于将可重复任务封装为可调用能力，可同时包含 Markdown 说明、参考资料、脚本和模板等配套文件。

例如，生成发布说明 Skill 可同时包含执行步骤、输出格式模板和版本号检查脚本。调用 Skill 后，Agent 将获得该 Skill 的说明及其资源范围。

### 7.1 支持的能力

Skill 模块支持：

- 在 Ora 中创建仅包含名称、描述和 Markdown 内容的 Skill；
- 导入单个 Skill 或一次导入多个 Skill；
- 导入文件夹、`.zip`、`.skill`、`.tar.gz` 或 `.tgz`；
- 查看和编辑 Ora 管理的 Skill；
- 删除无效或不再需要的 Skill；
- 对同名 Skill 分别选择跳过或覆盖；
- 在会话快捷操作中调用；
- 在工作流的 Agent 节点中启用或停用。

由插件提供的 Skill 属于插件内容，通常为只读。需要修改时，应修改插件来源或创建一个新的自定义 Skill。

### 7.2 Skill 目录结构

每个 Skill 是一个独立目录，目录根部必须有名称大小写完全一致的 `SKILL.md`。

```text
release-notes/
├─ SKILL.md
├─ references/
│  └─ style-guide.md
├─ scripts/
│  └─ check-version.js
└─ templates/
   └─ release-note.md
```

除 `SKILL.md` 外，其他目录名称不作强制规定，可根据 Skill 的实际需要组织资源。Ora 将 `SKILL.md` 所在目录识别为该 Skill 的边界。

如果导入的文件夹或压缩包中包含多个 `SKILL.md`，Ora 会把它们识别为多个 Skill。子目录中的另一个 `SKILL.md` 会形成新的 Skill，不再作为父 Skill 的普通附件。

### 7.3 `SKILL.md` 格式

`SKILL.md` 的第一部分必须是 YAML frontmatter，至少包含 `name` 和 `description`。其后的 Markdown 正文是给 Agent 使用的技能说明。

```markdown
---
name: release-notes
description: Creates release notes from repository changes
---

# Instructions

1. Read the changes since the previous release.
2. Group user-visible changes by category.
3. Follow the format in `templates/release-note.md`.
4. Do not include internal refactors unless they affect users.
```

主要限制：

- `name` 只能使用 ASCII 字母、数字、点、下划线和连字符；
- `name` 不能以点开头，最长 255 字节；
- `description` 不能为空，最长 4096 个 UTF-8 字节；
- 单个 `SKILL.md` 最大 1 MiB；
- 一个 Skill 最多包含 1000 个普通文件；
- 单次导入最多识别 500 个 Skill。

Skill 名称示例：

| 名称             | 是否有效 | 原因                  |
| ---------------- | -------- | --------------------- |
| `release-notes`  | 是       | 使用允许的字符        |
| `code_review.v2` | 是       | 点和下划线可用        |
| `.private-skill` | 否       | 不能以点开头          |
| `发布说明`       | 否       | 名称只允许 ASCII 字符 |
| `release notes`  | 否       | 不允许空格            |

### 7.4 导入和冲突处理

导入来源可以直接是 Skill 文件夹，也可以是支持的压缩包。压缩包内不要求只有一个 Skill，但每个候选 Skill 都必须有合法的 `SKILL.md`。

一次导入中，某个 Skill 格式错误不会阻止其他合法 Skill 被识别。Ora 会单独报告无效项。遇到同名项时，可以逐项选择跳过或覆盖。

如果列表中显示 Skill 不可用，通常表示记录仍存在，但本地 Skill 目录或根部 `SKILL.md` 已丢失。可以重新导入同名 Skill 进行恢复，或删除失效记录。

### 7.5 Skill 如何生效

在普通会话中，从快捷操作的 Skills 列表选择 Skill，即可把该 Skill 作为本次请求的可用能力。Skill 是否能完成任务，还取决于当前 Agent 是否具备它所需的工具和运行环境。

在工作流中，Skill 配置在 Agent 节点上，并可分别启用或停用。修改 Skill 后，新执行会使用更新后的内容；已产生的历史输出不会被重新计算。

## 8. 插件

插件用于向 Ora 增加原本不存在的运行能力。插件类型包括 Agent、Skill、MCP、Workbench 和 Webview；不同类型分别提供执行器、技能、工具连接或界面页面。

### 8.1 支持的能力

插件模块支持：

- 浏览、搜索和筛选插件目录；
- 安装市场插件；
- 从本地 `.orax` 文件导入插件；
- 查看插件版本、类型和说明；
- 启用、配置、重新加载和卸载插件；
- 管理插件市场源；
- 在会话中选择可用插件。

插件配置由插件自身声明，因此不同插件显示的配置项不同。配置对 Ora 中所有项目共享，不是只对当前项目生效。离开有未保存修改的配置页时，Ora 会要求保存、放弃或取消离开。

### 8.2 本地插件包格式

本地导入文件使用 `.orax` 扩展名，实际为 ZIP 压缩包。压缩包根目录必须包含 `orax.toml`，不能再多套一层文件夹。

```text
my-plugin.orax
├─ orax.toml
├─ ...该类型插件需要的文件
└─ assets/
```

`orax.toml` 至少需要声明插件标识、命名空间、类型、版本和说明。简化示例如下：

```toml
identifier = "my-agent"
namespace = "example"
kind = "agent"
version = "1.0.0"
description = "Provides an Agent for Ora"
```

`kind` 可用值为 `agent`、`skill`、`mcp`、`workbench` 或 `webview`。不同类型还需要各自的运行文件或清单字段；仅有上面的基础字段并不代表插件已经可运行。

Ora 会校验压缩包、根部清单、版本和对应类型的包结构。相同命名空间、标识和版本的插件不能重复安装。

### 8.3 安装、配置和卸载的影响

- 安装 Agent 插件后，其 Agent 和模型会在会话选择器中出现；是否可用取决于插件配置和本地运行环境。
- 安装 Skill 插件后，其 Skill 会进入可用 Skill 列表，并以只读内容管理。
- 安装 MCP 插件后，它提供的工具可由支持的 Agent 或工作流使用。
- 重置插件配置会移除用户覆盖值，插件声明的默认值仍然存在。
- 卸载插件可能同时删除其配置数据，并使依赖该插件的 Agent、Skill 或工具不可用。

卸载前应先确认是否有正在使用该插件的会话或工作流。已有历史记录不会因此消失，但后续执行可能失败或要求重新选择能力。

### 8.4 插件市场源

Ora 插件市场分为公司 codehub 和 GitHub，配置方式如下图所示：

**黄区插件市场地址：**https://codehub-y.huawei.com/AI_Coding_Lab/ora-space-marketplace/files

**分支：**master

GitHub插件市场地址：默认已配置

![image-20260902094615792](C:\Users\mds\AppData\Roaming\Typora\typora-user-images\image-20260902094615792.png)

![](C:\Users\mds\AppData\Roaming\Typora\typora-user-images\image-20260828104929302.png)

**注意事项：**

1、选择 GitHub 为插件市场源，由于公司网络无法直接访问 GitHub 的环境，需要在 Settings 的代理模块中配置网络代理（**配置方式在第9章**），Ora 才能访问 GitHub 并加载插件市场内容，插件市场源启用使用代理后，才可访问 GitHub插件市场

2、选择 codehub 为插件市场源，在黄区配置市场源后则可以直接同步插件市场

## 9. 网络代理

网络代理配置方式如下图所示，相关信息输入完成后点击保存即可：

![image-20260902094804658](C:\Users\mds\AppData\Roaming\Typora\typora-user-images\image-20260902094804658.png)

## 10. 工作流（Alpha）

### 10.1 节点能力

当前工作流只允许使用Start、agent节点。Agent 节点可以独立配置 Agent、模型、角色、Skill、MCP、附加说明和输出策略。因此，同一个工作流中的不同 Agent 节点可以承担不同职责。

### 10.2 草稿、发布与运行

工作流编辑内容先保存为草稿。草稿可持续修改，但不会直接出现在“运行工作流”入口中。

发布会生成一个可运行版本。已发布版本保持不变；后续修改会进入新的草稿。版本管理支持查看历史版本、激活指定版本、从历史版本恢复以及删除不再需要的发布版本。

运行工作流时使用的是当前激活的发布版本，而不是尚未发布的草稿。因此修改流程后，如果运行结果没有变化，应先检查是否已经重新发布并激活新版本。

### 10.3 导入与导出

工作流使用 Ora 导出的 `.reactflow.json` 文件交换。导出文件名通常为：

```text
<工作流名称>.reactflow.json
```

导入文件必须是有效 JSON，并包含有效的工作流名称和图结构。导入成功后，Ora 会创建该工作流并生成可运行的发布版本。

不要手工把任意 JSON 改名为 `.reactflow.json`。节点类型、连线和节点配置必须符合 Ora 的工作流结构，否则导入会失败或被规范化后丢弃无效内容。

## 11. 其他设置

### 11.1 隐私与历史记录

隐私设置用于控制历史记录保留和清理。清除历史属于不可逆操作，会影响已有会话记录，但不会删除项目源文件。

### 11.2 开发者选项

开发者选项可以设置日志等级，不同的日志等级显示不同的信息量，信息量随着Trace—>Debug—>Info—>Warm—>Error逐渐递减

## 12. 格式速查

| 内容   | 导入形式                                    | 必需入口文件                 | 关键要求                                              |
| ------ | ------------------------------------------- | ---------------------------- | ----------------------------------------------------- |
| 角色   | 单个 `.md`                                  | 该 Markdown 文件             | YAML frontmatter 含 `name`、`description`；最大 1 MiB |
| Skill  | 文件夹、`.zip`、`.skill`、`.tar.gz`、`.tgz` | 每个 Skill 根部的 `SKILL.md` | 名称字符受限；`SKILL.md` 最大 1 MiB                   |
| 插件   | `.orax`                                     | 压缩包根部 `orax.toml`       | ZIP 包；清单和对应类型结构必须有效                    |
| 工作流 | `.reactflow.json`                           | JSON 文件本身                | 必须包含有效名称和工作流图结构                        |

## 13. 常见问题

本节列出影响功能使用的常见问题及其检查方法。

### 13.1 GitHub插件市场为空或无法加载

**可能原因**：当前网络无法直接访问 GitHub，或代理配置未被插件市场源使用。

**处理方式**：

1. 在 Settings 的代理模块中填写可访问 GitHub 的代理主机和端口；代理要求身份验证时，同时填写用户名和密码。
2. 确认对应插件市场源已启用使用代理。
3. 保存设置并重新加载插件市场。
4. 如仍无法加载，应确认该代理在当前公司网络中能够访问 GitHub。

代理设置用于 Ora 访问插件市场所需的 GitHub 网络连接，不修改系统全局代理。

### 13.2 Agent 或模型不可用

应检查提供该 Agent 的插件是否已安装并启用，插件要求的登录信息、密钥或本地运行环境是否已经配置，以及当前 Agent 是否提供所选模型。

### 13.3 无法创建工作树任务

工作树任务要求项目目录为有效 Git 仓库，并且存在可用的基础分支。应检查 Git 仓库状态、基础分支以及本机 Git worktree 功能。非 Git 项目只能创建直接任务。

### 13.4 Skill 导入后未被识别

应检查以下内容：

- Skill 根目录是否存在大小写完全一致的 `SKILL.md`；
- `SKILL.md` 是否以 YAML frontmatter 开头；
- frontmatter 是否包含非空的 `name` 和 `description`；
- `name` 是否只使用允许的 ASCII 字符；
- 导入包格式、文件数量和文件大小是否超过限制。

压缩包可以包含多个 Skill。某个候选项无效时，应根据导入结果定位具体失败项。

### 13.5 Skill 未出现在会话快捷操作中

应确认 Skill 已成功导入且状态可用。Skill 记录存在但本地 `SKILL.md` 已丢失时，可重新导入同名 Skill 或删除失效记录。

### 13.6 角色导入失败

角色导入仅接受单个 Markdown 文件。文件开头必须包含 `name` 和 `description` YAML frontmatter，且文件大小不得超过 1 MiB。检测到同名角色时，需要明确选择跳过或覆盖。

### 13.7 本地插件导入失败

应检查 `.orax` 文件是否为有效 ZIP 包、`orax.toml` 是否位于压缩包根目录、清单字段和对应插件类型的包结构是否有效，以及相同命名空间、插件标识和版本是否已经安装。

### 13.8 工作流未出现在运行入口中

工作流草稿不能直接运行。应发布草稿并确认目标版本已激活。修改已发布工作流后，需要再次发布新版本；仅保存草稿不会改变当前运行版本。

### 13.9 工作流导入失败

导入文件必须为 Ora 导出的 `.reactflow.json`，并包含有效的工作流名称、节点和连线结构。修改其他 JSON 文件的扩展名不能将其转换为 Ora 工作流。

### 13.10 图片无法添加到会话

应检查图片格式和大小。支持 PNG、JPEG、WebP、GIF、AVIF 和 BMP；单张图片最大 5 MB，同一条消息中的图片总量最大 10 MB。当前 Agent 或模型还必须支持图片输入。

### 13.11 设置修改未生效

应确认配置已经保存。插件配置页面存在未保存内容时，离开页面会提供保存、放弃或取消选项。代理配置变更后，还需要重新加载插件市场以验证网络访问。

### 13.12 文件或变更内容异常

“文件”和“变更”始终以当前任务的工作目录为范围。内容与预期不一致时，应先确认当前选择的是直接任务还是工作树任务，再执行刷新。

“变更”无法加载时，应确认项目为有效 Git 仓库。“分支”和“已提交”范围只对具有任务基准提交的工作树任务开放。界面显示“没有变更”时，还应检查当前选择的差异范围是否正确。

### 13.13 提交或推送失败

提交说明不能为空。执行提交前，应确认工作区中的全部未提交变更均应纳入本次 commit。

推送失败时，应检查远程仓库配置、Git 凭据、网络连接及目标分支权限。公司网络限制 GitHub 访问时，插件市场代理配置不一定适用于 Git 命令；Git 推送仍需本机 Git 具备可用的网络和认证配置。
